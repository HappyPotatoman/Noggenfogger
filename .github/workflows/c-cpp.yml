name: C/C++ CI

on:
  push:
    branches: [ "master", "559942-with-bench" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: make
      run: make build
